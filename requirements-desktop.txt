# Desktop control-panel extras (installed on top of requirements.txt).
# The in-process production-grade WSGI server for the POS. The panel HTML is
# rendered in a NATIVE window via pywebview/WebView2 (an embedded control, not
# the Edge browser), with a chromeless Edge "--app" window as fallback. A tiny
# stdlib control server bridges the UI to the backend.
-r requirements.txt
waitress==3.0.2
# Native window: WebView2 ships with Windows 10/11; pythonnet 3.1+ has py3.14
# wheels. Falls back to Edge/browser if the WebView2 runtime is unavailable.
pywebview==6.2.1
pythonnet==3.1.0
# Only needed to BUILD the .exe, not to run from source. 6.15+ supports Py 3.14.
pyinstaller>=6.15
