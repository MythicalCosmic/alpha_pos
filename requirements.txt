Django==6.0.3
asgiref==3.11.1
sqlparse==0.5.5
redis==7.3.0
django-redis==6.0.0
requests==2.32.5
# Fernet encryption of the license key at rest + Ed25519 verification of
# perpetual-unlock files (licensing/services/crypto.py, unlock.py import it
# directly). Pinned explicitly — nothing else pulls it in transitively.
cryptography==48.0.0
psycopg2-binary==2.9.10
gunicorn==23.0.0
django-cors-headers==4.9.0
google-generativeai==0.8.5
